@model Bonyan.DAL.Models.Document
@{
    ViewData["Title"] = "رفع مستند";
    int projectId = ViewBag.ProjectId ?? 0;
    string projectName = ViewBag.ProjectName ?? "—";
}
<link rel="stylesheet" href="~/css/employee.css" />
<link rel="stylesheet" href="~/css/employee-dark.css" />
<link rel="stylesheet" href="~/css/document.css" />
<div class="page-header">
    <div class="page-header-left">
        <h2>
            <i class="fas fa-file-upload" style="color:var(--secondary)"></i>
            <span data-lang="upload_new_document">رفع مستند جديد</span>
        </h2>
        <p data-lang="doc_create_sub">رفع وإرفاق مستندات المشروع مع البيانات الوصفية</p>
    </div>
    <a asp-controller="Project" asp-action="Details"
       asp-route-id="@projectId" class="btn-bonyan-primary">
        <i class="fas fa-arrow-right"></i> <span data-lang="back_to_project">رجوع للمشروع</span>
    </a>
</div>
<div class="form-card">
    <div class="form-card-header">
        <i class="fas fa-file-alt"></i>
        <h4 data-lang="document_data">بيانات المستند</h4>
    </div>
    <div class="form-card-body">
        <div class="project-badge">
            <i class="fas fa-project-diagram"></i>
            @projectName
        </div>
        <form asp-action="Create" method="post"
              enctype="multipart/form-data">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="ProjectId" value="@projectId" />
            <div class="form-section-title" data-lang="file">الملف</div>
            <div class="form-group">
                <label data-lang="file">الملف</label> <span class="required">*</span>
                <div class="file-drop-zone" id="dropZone">
                    <i class="fas fa-cloud-upload-alt"></i>
                    <p data-lang="drag_drop_file">اسحب الملف هنا أو انقر للاختيار</p>
                    <span data-lang="file_size_hint">PDF, Word, Excel, PNG, JPG — الحجم الأقصى 20MB</span>
                    <input type="file" name="uploadedFile" id="fileInput"
                           onchange="previewFile(this)" />
                </div>
                <div class="file-preview" id="filePreview">
                    <i class="fas fa-file" id="fileIcon"></i>
                    <span class="file-name" id="fileName"></span>
                </div>
                <span class="text-danger">
                    @Html.ValidationMessage("uploadedFile")
                </span>
            </div>
            <div class="form-section-title" data-lang="data">البيانات</div>
            <div class="form-grid-2">
                <div class="form-group">
                    <label data-lang="doc_title">عنوان المستند</label> <span class="required">*</span>
                    <input asp-for="DocTitle" class="form-control"
                           data-lang-placeholder="doc_title_placeholder" placeholder="أدخل عنوان وصفياً للمستند" required />
                    <span asp-validation-for="DocTitle" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label data-lang="category">التصنيف</label>
                    <select asp-for="Category" class="form-select">
                        <option value="" data-lang="choose_category_option">-- اختر التصنيف --</option>
                        <option value="عقود" data-lang="cat_contracts">عقود</option>
                        <option value="تقارير" data-lang="cat_reports">تقارير</option>
                        <option value="مخططات" data-lang="cat_drawings">مخططات</option>
                        <option value="ملاحظات" data-lang="cat_notes">ملاحظات</option>
                        <option value="أخرى" data-lang="cat_other">أخرى</option>
                    </select>
                </div>
                <div class="form-group">
                    <label data-lang="notes">ملاحظات</label>
                    <textarea asp-for="Notes" class="form-control"
                              rows="3"
                              data-lang-placeholder="extra_notes_placeholder" placeholder="أي ملاحظات إضافية..."></textarea>
                </div>
            </div>
            <div class="form-actions">
                <a asp-controller="Project" asp-action="Details"
                   asp-route-id="@projectId"
                   class="btn-bonyan-primary">
                    <i class="fas fa-times"></i> <span data-lang="cancel">إلغاء</span>
                </a>
                <button type="submit" class="btn-bonyan-secondary">
                    <i class="fas fa-upload"></i> <span data-lang="upload_document">رفع المستند</span>
                </button>
            </div>
        </form>
    </div>
</div>
@section Scripts {
    <script>
        function previewFile(input) {
            const preview  = document.getElementById('filePreview');
            const fileName = document.getElementById('fileName');
            const fileIcon = document.getElementById('fileIcon');
            if (input.files && input.files[0]) {
                const name = input.files[0].name;
                const ext  = name.split('.').pop().toLowerCase();
                const iconMap = {
                    pdf:  'fa-file-pdf',
                    doc:  'fa-file-word', docx: 'fa-file-word',
                    xls:  'fa-file-excel', xlsx: 'fa-file-excel',
                    png:  'fa-file-image', jpg: 'fa-file-image',
                    jpeg: 'fa-file-image', gif: 'fa-file-image'
                };
                fileIcon.className = 'fas ' + (iconMap[ext] || 'fa-file');
                fileName.textContent = name;
                preview.style.display = 'flex';
                document.getElementById('dropZone').style.borderColor =
                    'var(--secondary)';
            }
        }
        const zone = document.getElementById('dropZone');
        zone.addEventListener('dragover',  e => {
            e.preventDefault();
            zone.classList.add('dragover');
        });
        zone.addEventListener('dragleave', () => zone.classList.remove('dragover'));
        zone.addEventListener('drop',      e => {
            e.preventDefault();
            zone.classList.remove('dragover');
            const dt = e.dataTransfer;
            if (dt.files.length) {
                document.getElementById('fileInput').files = dt.files;
                previewFile(document.getElementById('fileInput'));
            }
        });
    </script>
}
