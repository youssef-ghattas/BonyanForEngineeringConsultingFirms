@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>تسجيل أدمن - بنيان</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/login.css" />
    <link rel="stylesheet" href="~/css/register.css" />
</head>
<body>
    <div class="register-wrapper">

        <!-- Left Panel -->
        <div class="login-left">
            <div class="logo-icon">
                <i class="fas fa-shield-alt"></i>
            </div>
            <h1>بنيان</h1>
            <div class="divider"></div>
            <p>تسجيل حساب أدمن جديد — هذه الصفحة محمية بكود سري خاص</p>
            <div style="margin-top:24px; background:rgba(200,146,42,0.15);
                        border-radius:12px; padding:16px;">
                <p style="color:#C8922A; font-size:0.85rem; margin:0">
                    <i class="fas fa-info-circle"></i>
                    يجب الحصول على الكود السري من مدير النظام قبل التسجيل
                </p>
            </div>
        </div>

        <!-- Right Panel -->
        <div class="login-right register-right">
            <h2>إنشاء حساب أدمن</h2>
            <p>أدخل بياناتك والكود السري للتسجيل</p>

            @if (ViewBag.Error != null)
            {
                <div class="error-alert">
                    <i class="fas fa-exclamation-circle"></i>
                    @ViewBag.Error
                </div>
            }

            @if (TempData["Success"] != null)
            {
                <div style="background:#EEFAF2; border:1px solid #C3EDD0;
                                border-radius:10px; padding:12px 16px; color:#28A745;
                                font-size:0.88rem; margin-bottom:20px;
                                display:flex; align-items:center; gap:8px;">
                    <i class="fas fa-check-circle"></i>
                    @TempData["Success"]
                </div>
            }

            <form method="post" asp-action="AdminRegister" asp-controller="Account">
                @Html.AntiForgeryToken()

                <!-- First + Last Name -->
                <div class="form-row-2">
                    <div>
                        <label class="form-label">الاسم الأول</label>
                        <div class="input-group">
                            <i class="fas fa-user input-icon"></i>
                            <input type="text" name="firstName" class="form-control"
                                   placeholder="الاسم الأول" required />
                        </div>
                    </div>
                    <div>
                        <label class="form-label">الاسم الأخير</label>
                        <div class="input-group">
                            <i class="fas fa-user input-icon"></i>
                            <input type="text" name="lastName" class="form-control"
                                   placeholder="الاسم الأخير" required />
                        </div>
                    </div>
                </div>

                <!-- Email -->
                <div>
                    <label class="form-label">البريد الإلكتروني</label>
                    <div class="input-group">
                        <i class="fas fa-envelope input-icon"></i>
                        <input type="email" name="email" class="form-control"
                               placeholder="example@email.com" required />
                    </div>
                </div>

                <!-- Phone -->
                <div>
                    <label class="form-label">رقم الهاتف</label>
                    <div class="input-group">
                        <i class="fas fa-phone input-icon"></i>
                        <input type="tel" name="phoneNum" class="form-control"
                               placeholder="01xxxxxxxxx" />
                    </div>
                </div>

                <!-- Password -->
                <div>
                    <label class="form-label">كلمة المرور</label>
                    <div class="input-group">
                        <i class="fas fa-lock input-icon"></i>
                        <input type="password" name="password" class="form-control"
                               placeholder="أدخل كلمة المرور" required />
                    </div>
                </div>

                <!-- Secret Code -->
                <div>
                    <label class="form-label">
                        الكود السري
                        <span style="color:#DC3545; font-size:0.78rem; margin-right:6px">
                            (مطلوب من مدير النظام)
                        </span>
                    </label>
                    <div class="input-group">
                        <i class="fas fa-key input-icon"></i>
                        <input type="text" name="secretCode" class="form-control"
                               placeholder="أدخل الكود السري"
                               style="letter-spacing:3px; font-weight:700"
                               required />
                    </div>
                </div>

                <button type="submit" class="btn-login">
                    <i class="fas fa-shield-alt"></i>
                    إنشاء حساب أدمن
                </button>
            </form>

            <div class="switch-link">
                لديك حساب بالفعل؟
                <a asp-action="Login" asp-controller="Account">تسجيل الدخول</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>