@{
    ViewData["Title"] = "إنشاء حساب أدمن";
}

<link rel="stylesheet" href="~/css/employee.css" />

<!-- Page Header -->
<div class="page-header">
    <div class="page-header-left">
        <h2>
            <i class="fas fa-user-shield" style="color:var(--secondary)"></i>
            <span>إنشاء حساب أدمن جديد</span>
        </h2>
        <p>أدخل بيانات الأدمن الجديد</p>
    </div>
    <a asp-controller="Employee" asp-action="Index" class="btn-bonyan-primary">
        <i class="fas fa-arrow-right"></i>
        <span>رجوع</span>
    </a>
</div>

@if (ViewBag.Error != null)
{
    <div style="background:#FFF0F0; border:1px solid #FFCCCC; border-radius:10px;
                    padding:12px 18px; margin-bottom:18px; color:#DC3545;
                    display:flex; align-items:center; gap:10px; font-size:0.9rem;">
        <i class="fas fa-exclamation-circle"></i>
        @ViewBag.Error
    </div>
}

@if (TempData["Success"] != null)
{
    <div style="background:#EEFAF2; border:1px solid #C3EDD0; border-radius:10px;
                    padding:12px 18px; margin-bottom:18px; color:#28A745;
                    display:flex; align-items:center; gap:10px; font-size:0.9rem;">
        <i class="fas fa-check-circle"></i>
        @TempData["Success"]
    </div>
}

<div class="form-card">
    <div class="form-card-header">
        <i class="fas fa-user-shield"></i>
        <h4>بيانات الأدمن</h4>
    </div>

    <div class="form-card-body">
        <form asp-action="AdminRegister" asp-controller="Account" method="post">
            @Html.AntiForgeryToken()

            <!-- Personal Info -->
            <div class="form-section-title">المعلومات الشخصية</div>

            <div class="form-grid-2">
                <div class="form-group">
                    <label>
                        <span>الاسم الأول</span>
                        <span class="required">*</span>
                    </label>
                    <input type="text" name="firstName" class="form-control"
                           placeholder="الاسم الأول" required />
                </div>
                <div class="form-group">
                    <label>
                        <span>الاسم الأخير</span>
                        <span class="required">*</span>
                    </label>
                    <input type="text" name="lastName" class="form-control"
                           placeholder="الاسم الأخير" required />
                </div>
            </div>

            <div class="form-grid-2">
                <div class="form-group">
                    <label>
                        <span>البريد الإلكتروني</span>
                        <span class="required">*</span>
                    </label>
                    <input type="email" name="email" class="form-control"
                           placeholder="example@email.com" required />
                </div>
                <div class="form-group">
                    <label>رقم الهاتف</label>
                    <input type="tel" name="phoneNum" class="form-control"
                           placeholder="01xxxxxxxxx" />
                </div>
            </div>

            <!-- Account Info -->
            <div class="form-section-title" style="margin-top:10px">بيانات الحساب</div>

            <div class="form-grid-2">
                <div class="form-group">
                    <label>
                        <span>كلمة المرور</span>
                        <span class="required">*</span>
                    </label>
                    <input type="password" name="password" class="form-control"
                           placeholder="أدخل كلمة المرور" required />
                </div>
                <div class="form-group">
                    <label style="color:transparent">.</label>
                    <div style="background:#EEF4FF; border:1px solid #C7D8F5;
                                border-radius:10px; padding:12px 16px;
                                color:#1B3A6B; font-size:0.85rem; line-height:1.6;">
                        <i class="fas fa-info-circle" style="color:#C8922A"></i>
                        &nbsp;سيُطلب من الأدمن تغيير كلمة المرور عند أول تسجيل دخول
                    </div>
                </div>
            </div>

            <!-- Actions -->
            <div class="form-actions">
                <a asp-controller="Employee" asp-action="Index" class="btn-bonyan-primary">
                    <i class="fas fa-times"></i>
                    <span>إلغاء</span>
                </a>
                <button type="submit" class="btn-bonyan-secondary">
                    <i class="fas fa-user-shield"></i>
                    <span>إنشاء حساب أدمن</span>
                </button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}