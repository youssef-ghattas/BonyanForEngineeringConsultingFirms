@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>تسجيل الدخول - بنيان</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/login.css" />
    <link rel="stylesheet" href="~/css/auth-theme.css" />
</head>
<body>
    <script>
        (function () {
            const saved = localStorage.getItem('bonyan-dark');
            const prefersDark = window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches;
            if (saved === '1' || (saved === null && prefersDark)) document.body.classList.add('dark-mode');
        })();
    </script>
    <div class="login-wrapper">

        <!-- Left Panel -->
        <div class="login-left">
            <div class="logo-icon">
                <i class="fas fa-building"></i>
            </div>
            <h1>بنيان</h1>
            <div class="divider"></div>
            <p>منصة متكاملة لإدارة شركات الاستشارات الهندسية وتبسيط عمليات إدارة المشاريع والوثائق</p>
        </div>

        <!-- Right Panel -->
        <div class="login-right">
            <h2>مرحباً بك 👋</h2>
            <p>قم بتسجيل الدخول للمتابعة</p>

            @if (TempData["Success"] != null)
            {
                <div class="auth-success-alert">
                    <i class="fas fa-check-circle"></i>
                    @TempData["Success"]
                </div>
            }

            @if (ViewBag.Error != null)
            {
                <div class="error-alert">
                    <i class="fas fa-exclamation-circle"></i>
                    @ViewBag.Error
                </div>
            }

            <form method="post" asp-action="Login" asp-controller="Account">
                @Html.AntiForgeryToken()

                <div>
                    <label class="form-label">البريد الإلكتروني</label>
                    <div class="input-group">
                        <i class="fas fa-envelope input-icon"></i>
                        <input type="email" name="email" class="form-control"
                               placeholder="أدخل بريدك الإلكتروني" required />
                    </div>
                </div>

                <div>
                    <label class="form-label">كلمة المرور</label>
                    <div class="input-group">
                        <i class="fas fa-lock input-icon"></i>
                        <input type="password" name="password" id="loginPassword"
                               class="form-control" placeholder="أدخل كلمة المرور" required />
                        <i class="fas fa-eye toggle-password" data-target="loginPassword"></i>
                    </div>
                </div>

                <button type="submit" class="btn-login">
                    <i class="fas fa-sign-in-alt"></i>
                    تسجيل الدخول
                </button>
            </form>

            <div class="switch-link" style="margin-top:12px;">
                <a asp-action="ForgotPassword" asp-controller="Account">
                    <i class="fas fa-key"></i>
                    نسيت كلمة المرور؟
                </a>
            </div>

            <div class="switch-link">
                ليس لديك حساب؟
                <a asp-action="AdminRegister" asp-controller="Account">إنشاء حساب جديد</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/show-password.js"></script>
    <script src="~/js/auth-theme.js"></script>
</body>
</html>